/// Android Activity Recognition plugin for SmartNPS360.
///
/// Dart API lives in the app (`MotionActivityService`). This package exists so
/// the native channels register on every FlutterEngine, including the
/// `flutter_background_service` isolate.
library smartnps_motion_activity;
